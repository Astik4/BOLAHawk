# Signing secret for the deliberately vulnerable target API.
#
# This target exists to be broken. The JWT weak-secret check in the scanner
# looks for common wordlist entries, so keep this weak on purpose when you
# want that finding to reproduce. Never reuse this pattern in real software.
JWT_SECRET=secret
PORT=5000
HOST=0.0.0.0
