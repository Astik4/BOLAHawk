# Backend Scanning Tool Configuration
PORT=8000
HOST=127.0.0.1

# Vulnerable Target API configuration for scanning
TARGET_API_URL=http://127.0.0.1:5000

# Comma-separated list of allowed frontend origins for CORS.
# Defaults to http://localhost:5173 (Vite dev server) if not set.
# Example for production: ALLOWED_ORIGINS=https://your-frontend.vercel.app
ALLOWED_ORIGINS=http://localhost:5173
